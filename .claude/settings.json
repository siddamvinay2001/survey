{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in *.ts|*.tsx|*.js|*.jsx|*.mjs|*.cjs|*.json|*.jsonc) [ -f \"$f\" ] && pnpm exec biome check --write \"$f\" ;; esac; } 2>/dev/null || true",
            "statusMessage": "biome check --write"
          }
        ]
      }
    ]
  }
}
