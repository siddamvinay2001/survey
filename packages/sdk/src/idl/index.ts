// TODO(MVP-1): replace this stub with the generated IDL produced by
// `anchor build`. The generated file lives at
// packages/anchor/target/types/survey.ts after a build; copy it here and
// re-export so apps can import @survey/sdk/idl without depending on the
// anchor target directory.

import type { Idl } from '@coral-xyz/anchor';

export type SurveyIdl = Idl & {
  metadata: { name: 'survey'; version: '0.1.0'; spec: '0.1.0' };
};
